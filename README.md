# How to run
Sync all dependencies to local
```python
$ uv sync
```

create the virtual environemnt
```python
$ uv venv
```

sync dependencies
```python
$ uv sync
```

activate the virtual environment
```python
$ source .venv/bin/activate
```

add a package
```python
$ uv add
```
